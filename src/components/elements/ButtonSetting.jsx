import { createMuiTheme } from '@material-ui/core/styles';

const ButtonSetting = createMuiTheme({
    typography: {
        button: {
            textTransform: 'none'
        }
    }
});

export default ButtonSetting;